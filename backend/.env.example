# Backend environment variables (Render)
# Copy to .env in local dev, or set in Render Dashboard > Environment

# Core
PORT=5000
MONGO_URI=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/zarvo?retryWrites=true&w=majority
JWT_SECRET=change_me_to_a_strong_secret

# Email (Gmail App Password recommended)
EMAIL_USER=your.gmail@example.com
EMAIL_PASS=your_gmail_app_password
BUSINESS_EMAIL=optional_notifications@example.com

# Frontend origins for CORS and Socket.IO (comma-separated)
# e.g. https://your-frontend.vercel.app,https://staging-your-frontend.vercel.app
FRONTEND_URLS=http://localhost:5173,http://localhost:8080
