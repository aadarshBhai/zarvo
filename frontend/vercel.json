{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm ci || npm install",
  "outputDirectory": "dist",
  "framework": "vite",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/" }
  ]
}
